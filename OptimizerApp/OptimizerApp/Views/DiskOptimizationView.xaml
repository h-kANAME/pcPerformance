<UserControl x:Class="OptimizerApp.Views.DiskOptimizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <UserControl.Resources>
        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource RazerDarkGrayBrush}" />
            <Setter Property="Foreground" Value="{StaticResource RazerTextPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource RazerMediumGrayBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                        Template="{DynamicResource ComboBoxToggleButtonTemplate}"
                                        Grid.Column="2"
                                        Focusable="false"
                                        IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                        ClickMode="Press"
                                        Background="{StaticResource RazerDarkGrayBrush}"
                                        BorderBrush="{StaticResource RazerMediumGrayBrush}" />
                            <ContentPresenter x:Name="ContentPresenter"
                                            IsHitTestVisible="false"
                                            Content="{TemplateBinding SelectionBoxItem}"
                                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                            Margin="{TemplateBinding Padding}"
                                            VerticalAlignment="Center" />
                            <TextBox x:Name="PART_EditableTextBox"
                                    Style="{x:Null}"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    Margin="{TemplateBinding Padding}"
                                    Focusable="True"
                                    Visibility="Hidden"
                                    IsReadOnly="{TemplateBinding IsReadOnly}"
                                    Background="Transparent"
                                    Foreground="{StaticResource RazerTextPrimaryBrush}" />
                            <Popup x:Name="PART_Popup"
                                  AllowsTransparency="true"
                                  Placement="Bottom"
                                  IsOpen="{TemplateBinding IsDropDownOpen}"
                                  PopupAnimation="Slide">
                                <Grid x:Name="DropDown"
                                     SnapsToDevicePixels="True"
                                     MinWidth="{TemplateBinding ActualWidth}"
                                     MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                           BorderBrush="{StaticResource RazerMediumGrayBrush}"
                                           BorderThickness="1"
                                           Background="{StaticResource RazerDarkGrayBrush}">
                                        <ScrollViewer SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True"
                                                       KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="false">
                    <Setter Property="Opacity" Value="0.5" />
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <ControlTemplate x:Key="ComboBoxToggleButtonTemplate" TargetType="ToggleButton">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="20" />
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1,1,0,1" />
                <Border Grid.Column="1" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" />
                <Path Grid.Column="1" Data="M 0 0 L 4 4 L 8 0 Z" Stroke="{StaticResource RazerTextPrimaryBrush}" StrokeThickness="2" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Grid>
        </ControlTemplate>
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Text="Optimización de Disco" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource RazerTextPrimaryBrush}" />

            <!-- Status and Drive Selection -->
            <Grid Grid.Row="1" Margin="0,16,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Status Info -->
                <StackPanel Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="0">
                    <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource RazerStatusStyle}" FontSize="12" TextWrapping="Wrap" />
                </StackPanel>

                <!-- Drive Selection Label and ComboBox -->
                <StackPanel Grid.Column="0" Grid.Row="1">
                    <TextBlock Text="Seleccionar Unidad:" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <ComboBox ItemsSource="{Binding AvailableDrives, Mode=OneWay}" 
                              SelectedItem="{Binding SelectedDrive, Mode=TwoWay}" 
                              Height="30"
                              Style="{StaticResource DarkComboBox}"
                              IsEnabled="{Binding IsOperationInProgress, Converter={StaticResource BoolInverterConverter}}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding DriveLetter}" Margin="0,0,8,0" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding Name}" Margin="0,0,8,0" />
                                    <TextBlock Text="{Binding FreeGb, StringFormat='({0:F1} GB libres)'}" Foreground="{StaticResource AccentHighlightBrush}" />
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>

                <!-- Spacer -->
                <StackPanel Grid.Column="1" Grid.Row="1" />

                <!-- Control Buttons -->
                <StackPanel Grid.Column="2" Grid.Row="1" Orientation="Horizontal">
                    <StackPanel.Resources>
                        <Style TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
                            <Setter Property="Margin" Value="0,0,8,0" />
                            <Setter Property="Padding" Value="12,8" />
                            <Setter Property="MinWidth" Value="100" />
                        </Style>
                    </StackPanel.Resources>
                    <Button Content="Refrescar" Command="{Binding RefreshDrivesCommand}" IsEnabled="{Binding IsOperationInProgress, Converter={StaticResource BoolInverterConverter}}" />
                    <Button Content="Limpiar Temp" Command="{Binding CleanTempCommand}" />
                    <Button Content="TRIM" Command="{Binding ExecuteTrimCommand}" 
                            Visibility="{Binding CanExecuteTrim, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <Button Content="Desfragmentar" Command="{Binding DefragmentCommand}" 
                            Visibility="{Binding CanDefragment, Converter={StaticResource BoolToVisibilityConverter}}" />
                </StackPanel>
            </Grid>

            <!-- Drive Info -->
            <Border Grid.Row="2" Style="{StaticResource RazerBorderStyle}" Margin="0,0,0,12"
                    Visibility="{Binding SelectedDrive, Converter={StaticResource NullToVisibilityConverter}}">
                <StackPanel Margin="12">
                    <TextBlock Text="Información de la Unidad" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,12" Style="{StaticResource RazerSubtitleStyle}" />
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Letra:" FontWeight="SemiBold" Margin="0,0,16,8" />
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelectedDrive.DriveLetter}" Margin="0,0,0,8" />

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Nombre:" FontWeight="SemiBold" Margin="0,0,16,8" />
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedDrive.Name}" Margin="0,0,0,8" />

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Tipo:" FontWeight="SemiBold" Margin="0,0,16,8" />
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedDrive.DriveType}" Margin="0,0,0,8" />

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Uso:" FontWeight="SemiBold" Margin="0,0,16,8" />
                        <ProgressBar Grid.Row="3" Grid.Column="1" Height="12" Value="{Binding SelectedDrive.UsagePercentage, Mode=OneWay}" 
                                    Foreground="{StaticResource AccentHighlightBrush}" Margin="0,0,0,8" />

                        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal">
                            <TextBlock Text="{Binding SelectedDrive.UsedGb, StringFormat='{}{0:F2} GB'}" Margin="0,0,8,0" />
                            <TextBlock Text="/" Margin="0,0,8,0" />
                            <TextBlock Text="{Binding SelectedDrive.TotalGb, StringFormat='{}{0:F2} GB'}" Margin="0,0,8,0" />
                            <TextBlock Text="{Binding SelectedDrive.UsagePercentage, StringFormat='({0:F1}%)'}" 
                                      Foreground="{StaticResource AccentHighlightBrush}" />
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Operation Report -->
            <Border Grid.Row="4" Style="{StaticResource RazerBorderStyle}" Margin="0,0,0,0"
                    Visibility="{Binding HasReport, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel Margin="12">
                    <TextBlock Text="Informe de Operación" FontWeight="SemiBold" FontSize="14" 
                               Margin="0,0,0,12" Style="{StaticResource RazerSubtitleStyle}" />
                    <TextBox Text="{Binding ReportContent, Mode=OneWay}" 
                             IsReadOnly="True"
                             TextWrapping="Wrap"
                             MinHeight="300"
                             Padding="12"
                             FontFamily="Consolas"
                             FontSize="11"
                             Foreground="{StaticResource AccentHighlightBrush}" />
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
