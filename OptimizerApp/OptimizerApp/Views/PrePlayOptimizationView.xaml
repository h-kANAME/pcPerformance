<UserControl x:Class="OptimizerApp.Views.PrePlayOptimizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Gestionar procesos" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource RazerTextPrimaryBrush}" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,12" HorizontalAlignment="Left">
            <StackPanel.Resources>
                <Style TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
                    <Setter Property="Margin" Value="0,0,8,0" />
                </Style>
            </StackPanel.Resources>
            <Button Content="Analizar" Command="{Binding AnalyzeCommand}" />
            <Button Content="Optimizar" Command="{Binding OptimizeCommand}" />
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource RazerBorderStyle}" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="Estado antes" FontWeight="SemiBold" Style="{StaticResource RazerSubtitleStyle}" />
                    <TextBlock Text="{Binding Before.CpuUsagePercent, StringFormat=CPU {0}%}" />
                    <TextBlock Text="{Binding Before.RamUsagePercent, StringFormat=RAM {0}%}" />
                    <TextBlock Text="{Binding Before.DiskFreePercent, StringFormat=Disco libre {0}%}" />

                    <Separator Margin="0,8,0,8" />

                    <TextBlock Text="Estado después" FontWeight="SemiBold" />
                    <TextBlock Text="{Binding After.CpuUsagePercent, StringFormat=CPU {0}%}" />
                    <TextBlock Text="{Binding After.RamUsagePercent, StringFormat=RAM {0}%}" />
                    <TextBlock Text="{Binding After.DiskFreePercent, StringFormat=Disco libre {0}%}" />
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource RazerBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Aplicaciones y Procesos" FontWeight="SemiBold" Margin="0,0,0,8" Style="{StaticResource RazerSubtitleStyle}" />
                    <DataGrid ItemsSource="{Binding Candidates}" AutoGenerateColumns="False" IsReadOnly="True" MinHeight="180">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Nombre" Binding="{Binding Name}" Width="*" />
                            <DataGridCheckBoxColumn Header="Crítico" Binding="{Binding IsCritical}" Width="Auto" />
                            <DataGridTextColumn Header="Procesos" Binding="{Binding ProcessCount}" Width="Auto" />
                            <DataGridTextColumn Header="CPU (%)" Binding="{Binding CpuPercent}" Width="Auto" />
                            <DataGridTextColumn Header="Memoria (MB)" Binding="{Binding MemoryMb}" Width="Auto" />
                            <DataGridTextColumn Header="Descripción" Binding="{Binding Description}" Width="3*" />
                            <DataGridTextColumn Header="Ahorro estimado" Binding="{Binding SavingsDescription}" Width="2*" />
                            <DataGridTemplateColumn Header="Acción" Width="Auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Finalizar"
                                                Padding="8,4"
                                                Command="{Binding DataContext.CloseProcessCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="3" Style="{StaticResource RazerBorderStyle}" Margin="0,12,0,0">
            <StackPanel>
                <TextBlock Text="Resumen" FontWeight="SemiBold" Style="{StaticResource RazerSubtitleStyle}" />
                <TextBlock Text="{Binding LastRunSummary}" TextWrapping="Wrap" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

