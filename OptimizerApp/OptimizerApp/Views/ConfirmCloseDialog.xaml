<Window x:Class="OptimizerApp.Views.ConfirmCloseDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirmación"
        Width="520"
        Height="260"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource RazerBlackBrush}"
        Style="{StaticResource RazerWindowStyle}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource RazerBorderStyle}" Padding="12">
            <TextBlock x:Name="MessageText" 
                       TextWrapping="Wrap" 
                       FontSize="14"
                       Foreground="{StaticResource RazerTextPrimaryBrush}"
                       VerticalAlignment="Center" />
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <StackPanel.Resources>
                <Style TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
                    <Setter Property="Margin" Value="0,0,8,0" />
                </Style>
            </StackPanel.Resources>
            <Button Content="Cerrar" Width="110" Click="Close_Click" />
            <Button Content="Omitir" Width="110" Click="Skip_Click" />
            <Button Content="Continuar después" Width="150" Click="Stop_Click" />
        </StackPanel>
    </Grid>
</Window>

