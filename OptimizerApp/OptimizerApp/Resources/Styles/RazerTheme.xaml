<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Razer Color Palette -->
    <Color x:Key="RazerGreen">#44D62C</Color>
    <Color x:Key="RazerGreenDark">#00D800</Color>
    <Color x:Key="RazerGreenGlow">#66FF66</Color>
    <Color x:Key="RazerBlack">#0D0D0D</Color>
    <Color x:Key="RazerDarkGray">#1A1A1A</Color>
    <Color x:Key="RazerMediumGray">#2D2D2D</Color>
    <Color x:Key="RazerLightGray">#3D3D3D</Color>
    <Color x:Key="RazerTextPrimary">#E8E8E8</Color>
    <Color x:Key="RazerTextSecondary">#A0A0A0</Color>

    <!-- Brushes -->
    <SolidColorBrush x:Key="RazerGreenBrush" Color="{StaticResource RazerGreen}" />
    <SolidColorBrush x:Key="RazerGreenDarkBrush" Color="{StaticResource RazerGreenDark}" />
    <SolidColorBrush x:Key="RazerGreenGlowBrush" Color="{StaticResource RazerGreenGlow}" />
    <SolidColorBrush x:Key="RazerBlackBrush" Color="{StaticResource RazerBlack}" />
    <SolidColorBrush x:Key="RazerDarkGrayBrush" Color="{StaticResource RazerDarkGray}" />
    <SolidColorBrush x:Key="RazerMediumGrayBrush" Color="{StaticResource RazerMediumGray}" />
    <SolidColorBrush x:Key="RazerLightGrayBrush" Color="{StaticResource RazerLightGray}" />
    <SolidColorBrush x:Key="RazerTextPrimaryBrush" Color="{StaticResource RazerTextPrimary}" />
    <SolidColorBrush x:Key="RazerTextSecondaryBrush" Color="{StaticResource RazerTextSecondary}" />
    
    <!-- Accent Brush (para highlights y bordes de acento) -->
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource RazerGreen}" />
    <SolidColorBrush x:Key="AccentHighlightBrush" Color="{StaticResource RazerGreenGlow}" />

    <!-- Window Style -->
    <Style x:Key="RazerWindowStyle" TargetType="Window">
        <Setter Property="Background" Value="{StaticResource RazerBlackBrush}" />
        <Setter Property="Foreground" Value="{StaticResource RazerTextPrimaryBrush}" />
        <Setter Property="FontFamily" Value="Segoe UI, Arial" />
        <Setter Property="FontSize" Value="13" />
    </Style>

    <!-- Button Style -->
    <Style TargetType="Button">
        <Setter Property="Background" Value="{StaticResource RazerDarkGrayBrush}" />
        <Setter Property="Foreground" Value="{StaticResource RazerTextSecondaryBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource RazerMediumGrayBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="18,10" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter x:Name="content" 
                                        HorizontalAlignment="Center" 
                                        VerticalAlignment="Center"
                                        TextElement.Foreground="{TemplateBinding Foreground}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource RazerMediumGrayBrush}" />
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource RazerGreenBrush}" />
                            <Setter Property="Foreground" Value="{StaticResource RazerGreenGlowBrush}" />
                            <Setter TargetName="border" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="{StaticResource RazerGreen}" BlurRadius="12" ShadowDepth="0" Opacity="0.6" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource RazerBlackBrush}" />
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource RazerGreenBrush}" />
                            <Setter TargetName="border" Property="BorderThickness" Value="2" />
                            <Setter Property="Foreground" Value="{StaticResource RazerGreenBrush}" />
                            <Setter TargetName="border" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="{StaticResource RazerGreen}" BlurRadius="15" ShadowDepth="0" Opacity="0.8" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.3" />
                            <Setter Property="Foreground" Value="{StaticResource RazerTextSecondaryBrush}" />
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource RazerMediumGrayBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Primary Button Style (Verde Razer - para botones de acción) -->
    <Style x:Key="PrimaryButton" TargetType="Button">
        <Setter Property="Background" Value="#0D0D0D" />
        <Setter Property="Foreground" Value="#FFFFFF" />
        <Setter Property="BorderBrush" Value="{StaticResource RazerGreenBrush}" />
        <Setter Property="BorderThickness" Value="2" />
        <Setter Property="Padding" Value="18,10" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter x:Name="content" 
                                        HorizontalAlignment="Center" 
                                        VerticalAlignment="Center"
                                        TextElement.Foreground="{TemplateBinding Foreground}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#1A1A1A" />
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource RazerGreenGlowBrush}" />
                            <Setter TargetName="border" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="{StaticResource RazerGreen}" BlurRadius="15" ShadowDepth="0" Opacity="0.7" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#000000" />
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource RazerGreenBrush}" />
                            <Setter TargetName="border" Property="BorderThickness" Value="3" />
                            <Setter TargetName="border" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="{StaticResource RazerGreen}" BlurRadius="20" ShadowDepth="0" Opacity="0.9" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.3" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TextBlock Style -->
    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource RazerTextPrimaryBrush}" />
        <Setter Property="FontFamily" Value="Segoe UI, Arial" />
    </Style>

    <!-- TabControl Style -->
    <Style TargetType="TabControl">
        <Setter Property="Background" Value="{StaticResource RazerBlackBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource RazerMediumGrayBrush}" />
        <Setter Property="BorderThickness" Value="0" />
    </Style>

    <Style TargetType="TabItem">
        <Setter Property="Foreground" Value="{StaticResource RazerTextSecondaryBrush}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Padding" Value="20,10" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="Border" 
                            Background="{StaticResource RazerDarkGrayBrush}"
                            BorderBrush="{StaticResource RazerMediumGrayBrush}"
                            BorderThickness="1,1,1,0"
                            CornerRadius="6,6,0,0"
                            Margin="2,0,0,0"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter x:Name="ContentSite"
                                        ContentSource="Header"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource RazerBlackBrush}" />
                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource RazerGreenBrush}" />
                            <Setter TargetName="Border" Property="BorderThickness" Value="1,2,1,0" />
                            <Setter Property="Foreground" Value="{StaticResource RazerGreenBrush}" />
                            <Setter TargetName="Border" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="{StaticResource RazerGreen}" BlurRadius="8" ShadowDepth="0" Opacity="0.5" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource RazerGreenGlowBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Border Style -->
    <Style x:Key="RazerBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource RazerDarkGrayBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource RazerMediumGrayBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="6" />
        <Setter Property="Padding" Value="16" />
    </Style>

    <!-- DataGrid Style -->
    <Style TargetType="DataGrid">
        <Setter Property="Background" Value="{StaticResource RazerBlackBrush}" />
        <Setter Property="Foreground" Value="{StaticResource RazerTextPrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource RazerMediumGrayBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="RowBackground" Value="{StaticResource RazerDarkGrayBrush}" />
        <Setter Property="AlternatingRowBackground" Value="{StaticResource RazerBlackBrush}" />
        <Setter Property="GridLinesVisibility" Value="Horizontal" />
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource RazerMediumGrayBrush}" />
        <Setter Property="VerticalGridLinesBrush" Value="{StaticResource RazerMediumGrayBrush}" />
        <Setter Property="HeadersVisibility" Value="Column" />
        <Setter Property="CanUserResizeRows" Value="False" />
        <Setter Property="SelectionMode" Value="Single" />
        <Setter Property="AutoGenerateColumns" Value="False" />
    </Style>

    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{StaticResource RazerMediumGrayBrush}" />
        <Setter Property="Foreground" Value="{StaticResource RazerGreenBrush}" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="BorderBrush" Value="{StaticResource RazerGreenBrush}" />
        <Setter Property="BorderThickness" Value="0,0,0,2" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
    </Style>

    <Style TargetType="DataGridRow">
        <Setter Property="Background" Value="{StaticResource RazerDarkGrayBrush}" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource RazerMediumGrayBrush}" />
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource RazerLightGrayBrush}" />
                <Setter Property="Foreground" Value="{StaticResource RazerGreenBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="DataGridCell">
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="8,6" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <Border Background="{TemplateBinding Background}" 
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Separator Style -->
    <Style TargetType="Separator">
        <Setter Property="Background" Value="{StaticResource RazerGreenBrush}" />
        <Setter Property="Height" Value="1" />
        <Setter Property="Opacity" Value="0.3" />
    </Style>

    <!-- TextBox Style -->
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource RazerDarkGrayBrush}" />
        <Setter Property="Foreground" Value="{StaticResource RazerTextPrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource RazerMediumGrayBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="8,6" />
        <Setter Property="CaretBrush" Value="{StaticResource RazerGreenBrush}" />
        <Style.Triggers>
            <Trigger Property="IsFocused" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource RazerGreenBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ScrollBar Style -->
    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="{StaticResource RazerDarkGrayBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource RazerMediumGrayBrush}" />
    </Style>

    <!-- Title Text Style -->
    <Style x:Key="RazerTitleStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource RazerGreenBrush}" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="FontSize" Value="24" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="{StaticResource RazerGreen}" BlurRadius="8" ShadowDepth="0" Opacity="0.4" />
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Subtitle Text Style -->
    <Style x:Key="RazerSubtitleStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource RazerGreenBrush}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontSize" Value="16" />
    </Style>

    <!-- Status Text Style -->
    <Style x:Key="RazerStatusStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource RazerGreenGlowBrush}" />
        <Setter Property="FontFamily" Value="Consolas, Courier New" />
        <Setter Property="FontSize" Value="12" />
    </Style>

    <!-- CheckBox Style -->
    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource RazerTextPrimaryBrush}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal">
                        <Border x:Name="border" 
                                Width="20" Height="20" 
                                BorderBrush="{StaticResource RazerGreenBrush}" 
                                BorderThickness="2" 
                                CornerRadius="3"
                                Background="{StaticResource RazerDarkGrayBrush}"
                                Margin="0,0,8,0">
                            <TextBlock x:Name="check" 
                                     Text="✓" 
                                     FontSize="14" 
                                     FontWeight="Bold"
                                     Foreground="{StaticResource RazerGreenBrush}"
                                     HorizontalAlignment="Center" 
                                     VerticalAlignment="Center"
                                     Visibility="Collapsed" />
                        </Border>
                        <ContentPresenter VerticalAlignment="Center" />
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="check" Property="Visibility" Value="Visible" />
                            <Setter TargetName="border" Property="Background" Value="{StaticResource RazerBlackBrush}" />
                            <Setter TargetName="border" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="{StaticResource RazerGreen}" BlurRadius="8" ShadowDepth="0" Opacity="0.6" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource RazerGreenGlowBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ContextMenu Style -->
    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{StaticResource RazerDarkGrayBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource RazerMediumGrayBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground" Value="{StaticResource RazerTextPrimaryBrush}" />
        <Setter Property="FontFamily" Value="Segoe UI, Arial" />
        <Setter Property="FontSize" Value="13" />
    </Style>

    <!-- Secondary Button Style -->
    <Style x:Key="SecondaryButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource RazerDarkGrayBrush}" />
        <Setter Property="Foreground" Value="{StaticResource RazerTextSecondaryBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource RazerMediumGrayBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="18,10" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter x:Name="content" 
                                        HorizontalAlignment="Center" 
                                        VerticalAlignment="Center"
                                        TextElement.Foreground="{TemplateBinding Foreground}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource RazerMediumGrayBrush}" />
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource RazerMediumGrayBrush}" />
                            <Setter Property="Foreground" Value="{StaticResource RazerTextPrimaryBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource RazerLightGrayBrush}" />
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource RazerMediumGrayBrush}" />
                            <Setter Property="Foreground" Value="{StaticResource RazerTextPrimaryBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.3" />
                            <Setter Property="Foreground" Value="{StaticResource RazerTextSecondaryBrush}" />
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource RazerMediumGrayBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
